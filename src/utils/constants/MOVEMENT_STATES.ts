const MOVEMENT_STATES = {
	IDLE: 'Idle',
	WALKING: 'Walking',
	RUNNING: 'Running',
};

const MOVEMENT_STATES_ARRAY: string[] = Object.values(MOVEMENT_STATES);

export { MOVEMENT_STATES_ARRAY };
export default MOVEMENT_STATES;
